import 'package:flutter/material.dart';

BoxDecoration profileTypeDecoration = BoxDecoration(
   borderRadius: BorderRadius.circular(10.0),
   border: Border.all(
     color: Colors.white,
     style: BorderStyle.solid
   )
);